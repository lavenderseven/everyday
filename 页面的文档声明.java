//文档声明：(html、jsp)DOCTYPE
1:什么是DOCTYPE:
	document typ（文档类型）的简写，用来说明你用的XHTML或是HTML是什么版本
2:DOCTYPE的作用：
	声明文档的解析类型，避免浏览器的怪异模式，这个属性会被浏览器识别并使用，
	但是如果页面没有DOCTYPE的声明，那么compatMode默认就是backCompat,
	页面声明了<!DOCTYPE html>那么，等于开启了标准模式，按照W3C